name: Build and test 2023

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: bazelbuild/setup-bazelisk@v2
    - run: bazel test //2023/... --cxxopt='-std=c++17' --test_output=errors